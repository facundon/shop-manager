export enum Gender {
   Male = "male",
   Female = "female",
}

export enum OrderStatus {
   Pending = "pending",
   Progress = "in_progress",
   Finished = "finished",
   Delivered = "delivered",
   Canceled = "canceled",
}
